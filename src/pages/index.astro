---
import { projectConfig } from "@project/index";
---

<!doctype html>
<html lang={projectConfig.locale}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{projectConfig.name}</title>
  </head>
  <body>
    <main>
      <h1>{projectConfig.name}</h1>
      <p>Stage 1 repository skeleton.</p>
    </main>
  </body>
</html>
